# DO NOT commit real secrets.
# Add your OpenAI API key here for local development only.
# After rotating the compromised key, set the new key here locally or as an OS env var.
OPENAI_API_KEY=REPLACE_ME_WITH_OPENAI_KEY